-- add stock_quantity to products table for counting of stocks

ALTER TABLE products ADD COLUMN stock_quantity INT DEFAULT 0 AFTER price;

[DONE AND CHANGED]

-- new table for sizes connect to products
-- stock_quantity depends on sizes
-- create gender/product sections (like women mens section) column 
-- create age (kids, teens, adults)
-- create shoe sizes tables that connects to category (shoe)

-- [DONE]

CREATE TABLE shipping_addresses (
    address_id INT PRIMARY KEY AUTO_INCREMENT,
    customer_id INT,
    address_label VARCHAR(50), -- "Home", "Office", etc.
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    phone VARCHAR(20),
    address_line1 VARCHAR(255),
    address_line2 VARCHAR(255),
    city VARCHAR(50),
    province VARCHAR(50),
    postal_code VARCHAR(10),
    is_default TINYINT(1) DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (customer_id) REFERENCES customers(customer_id)
);
